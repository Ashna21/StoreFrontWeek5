@model IEnumerable<StoreFront.Models.Product>


<div id="ProductsList">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Picture)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img src="@Url.Content(item.ImageFile)" style="width: 100px; height: 100px; border-radius:50%" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductName)
                </td>
                <td>
                    $ @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.ActionLink("Add", "AddToCart", "ShoppingCarts", new { productId = item.ProductID, userId = Session["id"] }, null)
                </td>

            </tr>

        }
    </table>
</div>
S